const updateStatus = document.querySelector('#update-status');
updateStatus.innerHTML = ("
  <% unless @up_status == "completed"%>
    <%= button_to 'Mark complete', user_progress_path(step_id: step_id), method: :patch %>
  <% else %>
    <%= button_to 'Lesson Completed', user_progress_path(step_id: step_id), method: :patch %>
  <% end %>
");